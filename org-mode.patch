diff --git a/org-mode/org-mode.cabal b/org-mode/org-mode.cabal
index 051ab25..28f6dde 100644
--- a/org-mode.cabal
+++ b/org-mode.cabal
@@ -26,8 +26,8 @@ common commons
     -funclutter-valid-hole-fits

   build-depends:
-    , base        >=4.12 && <5
-    , megaparsec  >=7    && <9
+    , base
+    , megaparsec
     , text

 library
@@ -35,10 +35,10 @@ library
   hs-source-dirs:  lib
   exposed-modules: Data.Org
   build-depends:
-    , containers          >=0.6
+    , containers
     , filepath
-    , hashable            >=1.2 && <1.4
-    , parser-combinators  >=1.1 && <1.3
+    , hashable
+    , parser-combinators

 test-suite org-mode-test
   import:         commons
@@ -48,5 +48,5 @@ test-suite org-mode-test
   ghc-options:    -threaded -rtsopts -with-rtsopts=-N
   build-depends:
     , org-mode
-    , tasty        ^>=1.2
-    , tasty-hunit  ^>=0.10
+    , tasty
+    , tasty-hunit
